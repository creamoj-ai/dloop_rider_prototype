{
  "buildCommand": "$HOME/flutter/bin/flutter build web --release --web-renderer html",
  "outputDirectory": "build/web",
  "framework": null,
  "installCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && $HOME/flutter/bin/flutter doctor && $HOME/flutter/bin/flutter pub get",
  "github": {
    "silent": true
  }
}
